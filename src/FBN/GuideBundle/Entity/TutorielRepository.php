<?php

namespace FBN\GuideBundle\Entity;

use Doctrine\ORM\EntityRepository;
use Doctrine\ORM\Tools\Pagination\Paginator;

/**
 * TutorielRepository.
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class TutorielRepository extends EntityRepository
{
    public function getArticlesImages($first = 0, $limit = Article::NUM_ITEMS)
    {
        $qb = $this->createQueryBuilder('t')
                   ->leftJoin('t.image', 'i')
                   ->addSelect('i')
                   ->orderBy('t.datePublication', 'DESC')
                    ->where('t.publication = :publication')
                    ->setParameter('publication', 1);

        $query = $qb->getQuery();

        $query->setFirstResult($first)
              ->setMaxResults($limit);

        return new Paginator($query);
    }

    public function getTutoriel($slug)
    {
        $qb = $this->createQueryBuilder('t')
                   ->leftJoin('t.image', 'i')
                   ->addSelect('i')
                   ->leftJoin('t.tutorielRubrique', 'tr')
                   ->addSelect('tr')
                   ->leftJoin('t.tutorielChapitre', 'tc')
                   ->addSelect('tc')
                   ->leftJoin('tc.tutorielChapitrePara', 'tcp')
                   ->addSelect('tcp')
                    ->where('t.slug = :slug')
                    ->setParameter('slug', $slug);

        return $qb->getQuery()
                  ->getOneOrNullResult();
    }
}
